# Figures

This directory contains two types of assets:

- exported analysis figures copied from experiment artifacts
- paper figure source files copied from `paper/figures/`

The paper-source files are kept as `*_source.tex` so the main LaTeX build remains under `paper/`.
